export interface Message {
    name: string;
    phoneNumber: string;
    email: string;
    location: string;
    message: string;
}

export interface Booking {
    name: string;
    phoneNumber: string;
    email: string;
    location: string;
    message: string;
    date: string;
}